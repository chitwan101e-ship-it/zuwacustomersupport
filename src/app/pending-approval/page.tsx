'use client'

import { PendingApprovalPanel } from '@/components/PendingApprovalPanel'

/**
 * Customers who signed up stay here until staff approve (also embedded in signup step 4).
 */
export default function PendingApprovalPage() {
  return <PendingApprovalPanel />
}
